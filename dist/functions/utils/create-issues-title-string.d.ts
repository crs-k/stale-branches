/**
 * Creates title string for GitHub issues
 *
 * @param {string} branchName The name of a branch.
 *
 * @returns `[${branchName}] is STALE`
 */
export declare function createIssueTitleString(branchName: string): string;
